# router-replica-stunnel-v3 — client-side TLS to the Phala DB SNI endpoint.
# UNKNOWN: exact env var names — derived from stunnel.conf.template + entrypoint.sh.
# Confirm against railway/router-replica-stunnel/ before prod; not invented here.
PHALA_DB_SNI_ENDPOINT=CHANGEME_VERIFY_AGAINST_ENTRYPOINT
