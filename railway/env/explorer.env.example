# explorer-v3 — service-local. Minimal (static/SSR UI).
# UNKNOWN: upstream data source var (router-api vs gateway vs standby) — confirm
# against railway/explorer before prod; not invented here.
HOST=0.0.0.0
PORT=3000
