# redis-v3 — forever (no-TTL) decimals cache for router-gateway-v3.
# No service-local env is required (configuration is baked into railway/redis/Dockerfile).
# Attach a Railway volume mounted at /data so cached decimals persist across deploys.
