{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "etc/Dockerfile.bitcoin-indexer",
    "watchPatterns": [
      "bitcoin-indexer/**",
      "crates/**",
      "Cargo.toml",
      "Cargo.lock",
      "etc/Dockerfile.bitcoin-indexer"
    ]
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1
  }
}
