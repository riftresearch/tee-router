{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "etc/Dockerfile.hl-shim-indexer",
    "watchPatterns": [
      "hl-shim-indexer/**",
      "crates/**",
      "Cargo.toml",
      "Cargo.lock",
      "etc/Dockerfile.hl-shim-indexer"
    ]
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1
  }
}
