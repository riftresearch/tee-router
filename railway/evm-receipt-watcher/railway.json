{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "etc/Dockerfile.evm-receipt-watcher",
    "watchPatterns": [
      "evm-receipt-watcher/**",
      "crates/**",
      "Cargo.toml",
      "Cargo.lock",
      "etc/Dockerfile.evm-receipt-watcher"
    ]
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1
  }
}
