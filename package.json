{
  "name": "tee-router",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "router-gateway:dev": "bun --filter @rift/router-gateway dev",
    "router-gateway:start": "bun --filter @rift/router-gateway start",
    "router-gateway:typecheck": "bun --filter @rift/router-gateway typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.3.1",
    "typescript": "^5.9.3"
  }
}
