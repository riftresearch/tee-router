# Deployment Docs

The old OTC/RFQ deployment instructions were removed during the router cleanup.

What remains:

- `etc/compose.phala.yml` is preserved as historical reference material for Phala-hosted deployments.
- `deploy/railway/sauron/README.md` documents the current Sauron router observer shape.

There is no maintained router-server Docker release flow in this file yet.
